@*@model MVC_OES.Models.Questions.QuestionModel
    @{
        ViewBag.Title = "Edit Question";
        int courseId = ViewBag.CourseId;
        int levelNumber = ViewBag.LevelNumber;
    }

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

    <div class="container mt-4">
        <div class="card shadow-sm p-4">
            <h2 class="mb-3">Edit Question — Course @courseId | Level @levelNumber</h2>

            @if (TempData["ApiError"] != null)
            {
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle"></i> @TempData["ApiError"]
                </div>
            }

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.QuestionId)
                @Html.HiddenFor(m => m.CourseId)
                @Html.HiddenFor(m => m.LevelNumber)

                <div class="mb-3">
                    @Html.LabelFor(m => m.QuestionText, htmlAttributes: new { @class = "fw-bold" })
                    @Html.TextAreaFor(m => m.QuestionText, new { @class = "form-control", rows = 3 })
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.OptionA, htmlAttributes: new { @class = "fw-bold" })
                        @Html.TextBoxFor(m => m.OptionA, new { @class = "form-control" })
                    </div>
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.OptionB, htmlAttributes: new { @class = "fw-bold" })
                        @Html.TextBoxFor(m => m.OptionB, new { @class = "form-control" })
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.OptionC, htmlAttributes: new { @class = "fw-bold" })
                        @Html.TextBoxFor(m => m.OptionC, new { @class = "form-control" })
                    </div>
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.OptionD, htmlAttributes: new { @class = "fw-bold" })
                        @Html.TextBoxFor(m => m.OptionD, new { @class = "form-control" })
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.Answer, htmlAttributes: new { @class = "fw-bold" })
                        @Html.DropDownListFor(m => m.Answer,
                            new SelectList(new[] { "A", "B", "C", "D" }, Model.Answer), "Select Correct Answer",
                            new { @class = "form-control" })
                    </div>
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.Marks, htmlAttributes: new { @class = "fw-bold" })
                        @Html.TextBoxFor(m => m.Marks, new { @class = "form-control", type = "number", min = "1" })
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save"></i> Update Question
                    </button>
                    <a href="@Url.Action("GetQuestions", "Question", new { courseId = courseId, levelNumber = levelNumber })" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Cancel
                    </a>
                </div>
            }
        </div>
    </div>*@

@model MVC_OES.Models.Questions.QuestionModel

@{
    ViewBag.Title = "Edit Question";
    int courseId = ViewBag.CourseId;
    int levelNumber = ViewBag.LevelNumber;
}

<h2>Edit Question — Course @courseId | Level @levelNumber</h2>

@if (TempData["ApiError"] != null)
{
    <div class="alert alert-danger">@TempData["ApiError"]</div>
}

@using (Html.BeginForm())
{
    @Html.HiddenFor(m => m.QuestionId)
    @Html.HiddenFor(m => m.CourseId)
    @Html.HiddenFor(m => m.LevelNumber)

    <div class="mb-3">
        @Html.LabelFor(m => m.QuestionText)
        @Html.TextAreaFor(m => m.QuestionText, new { @class = "form-control", rows = 3 })
    </div>

    <div class="mb-3">
        @Html.LabelFor(m => m.OptionA)
        @Html.TextBoxFor(m => m.OptionA, new { @class = "form-control" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.OptionB)
        @Html.TextBoxFor(m => m.OptionB, new { @class = "form-control" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.OptionC)
        @Html.TextBoxFor(m => m.OptionC, new { @class = "form-control" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.OptionD)
        @Html.TextBoxFor(m => m.OptionD, new { @class = "form-control" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.Answer)
        @Html.TextBoxFor(m => m.Answer, new { @class = "form-control" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.Marks)
        @Html.TextBoxFor(m => m.Marks, new { @class = "form-control", type = "number", min = "1" })
    </div>

    <button type="submit" class="btn btn-success">Update Question</button>
    <a href="@Url.Action("GetQuestions", "Question", new { courseId = courseId, levelNumber = levelNumber })" class="btn btn-secondary">Cancel</a>
}

